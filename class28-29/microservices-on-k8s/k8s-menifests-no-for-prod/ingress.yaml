apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: craftista-ingress
  namespace: craftista
  annotations:
    # ISSUE: The line below is for NGINX Ingress Controller, but you have AWS Load Balancer Controller installed
    # This mismatch prevents the load balancer from being created
    # nginx.ingress.kubernetes.io/rewrite-target: /
    
    # FIX: Add this annotation to specify AWS Load Balancer Controller
    kubernetes.io/ingress.class: "alb"
    # Create an internet-facing ALB (public access)
    alb.ingress.kubernetes.io/scheme: "internet-facing"
    # Use IP mode for better compatibility with Fargate and pod networking
    alb.ingress.kubernetes.io/target-type: "ip"
    # Health check path - ALB will check this endpoint for service health
    alb.ingress.kubernetes.io/healthcheck-path: "/"
    # Listen only on port 80 (HTTP) - no SSL in this configuration
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTP":80}]'
spec:
  # FIX: Explicitly specify the ingress class for AWS Load Balancer Controller
  ingressClassName: alb
  rules:
  - host: ms.akhileshmishra.tech  # We'll map this in /etc/hosts
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend
            port:
              number: 80
      - path: /api/products
        pathType: Prefix
        backend:
          service:
            name: catalogue
            port:
              number: 5000
      - path: /api/origami-of-the-day
        pathType: Prefix
        backend:
          service:
            name: recco
            port:
              number: 8080
      - path: /api/origamis
        pathType: Prefix
        backend:
          service:
            name: voting
            port:
              number: 8080